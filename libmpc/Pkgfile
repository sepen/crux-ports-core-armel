# Depends on: libmpfr

name=libmpc
version=1.4.1
release=1
source=(https://ftp.gnu.org/gnu/mpc/mpc-$version.tar.xz)

build() {
    cd mpc-$version

    ./configure --prefix=/usr
    make
    make DESTDIR=$PKG install

    rm -r $PKG/usr/share
}
